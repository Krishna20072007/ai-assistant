class GPTClient:
    def __init__(self, api_key):
        self.api_key = api_key  # Store API key for requests

    def query(self, message):
        # Send the user's message to the AI API and return the response
        # (Implement your API call here, e.g., using requests)
        pass